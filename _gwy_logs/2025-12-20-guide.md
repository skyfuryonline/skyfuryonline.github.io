---
date: 2025-12-20 08:00:00
title: "备考记录使用指南 (2026 旗舰版)"
subjects:
  - name: 资料分析
    hours: 2.5
  - name: 判断推理
    hours: 3.5
  - name: 申论
    hours: 2
tags: [指南, 系统说明]
---

### 欢迎开启 2026 备考之旅！

这是您的备考记录系统 **2.0 版本** 的完整使用说明书。本系统旨在通过**精细化的数据统计**和**可视化的反馈**，辅助您科学备考。

---

#### 1. 如何记录？(核心)

您只需在 `_gwy_logs` 目录下创建一个新的 `.md` 文件（建议用日期命名），并填入以下内容。

**推荐模板 (直接复制)**:

```yaml
---
date: 2026-03-15 20:00:00
title: "今天攻克了图形推理"
subjects:
  - name: 资料分析
    hours: 2
  - name: 判断推理
    hours: 3.5
  - name: 言语理解
    hours: 0
  - name: 数量关系
    hours: 0
  - name: 申论
    hours: 1.5
  - name: 其他
    hours: 0
tags: [刷题, 复盘]
---

### 📝 今日复盘

今天重点复习了图形推理，发现自己对“一笔画”问题还是不够敏感...
```

**字段说明**:
*   **subjects**: 這是核心统计源。请在对应的科目下填写小时数。
*   **tags**: 仅作为标记（如“模考”、“听课”），**不**参与时长统计。

---

#### 2. 智能归类与映射系统

为了方便记录，系统内置了**关键词映射**功能。您在 `name` 字段中可以使用简称，系统会自动归类到标准大类。

| 您填写的 (简称) | 系统归类为 (标准大类) | 饼图颜色 |
| :--- | :--- | :--- |
| **资料**、速算 | **资料分析** | 🔵 蓝色 |
| **图推**、逻辑、类比 | **判断推理** | 🟣 紫色 |
| **言语**、片段阅读 | **言语理解** | 🟢 绿色 |
| **数学**、数量 | **数量关系** | 🟠 橙色 |
| **常识** | **常识判断** | 🟢 青色 |
| **大作文**、公文 | **申论** | 🔴 红色 |
| 面试、模拟 | 面试/模考 | 🟡 黄色/深蓝 |

*建议：尽量使用标准大类名称，或者固定的简称，以保持习惯一致。*

---

#### 3. 可视化图表解读

*   **月历卡片 (中间)**: 
    *   显示当月的打卡情况。
    *   **颜色深浅**代表当天的总学习时长 (0h 灰色 -> 6h+ 深绿)。
    *   点击月份两侧的箭头 `< >` 可以切换月份。
*   **最近7天趋势 (左侧上)**: 
    *   显示过去7天的总学习时长柱状图。
*   **科目分布 (左侧下)**: 
    *   显示本周（或根据日志累计）的各科目投入比例。
    *   颜色与上面的表格对应。

---

#### 4. 图片插入指南

**单张图片**:
```markdown
![我的笔记](/img/gwy/note.jpg)
```

**多图并排 (推荐)**:
使用我们封装好的组件，可以让 2-3 张图片完美并排显示。

```liquid
{% raw %}
<!-- 两张图 -->
{% include gwy_image_row.html 
    img1="/img/gwy/pic1.jpg" 
    img2="/img/gwy/pic2.jpg" 
%}

<!-- 三张图 -->
{% include gwy_image_row.html 
    img1="/img/gwy/pic1.jpg" 
    img2="/img/gwy/pic2.jpg" 
    img3="/img/gwy/pic3.jpg" 
%}
{% endraw %}
```

---

祝您备考顺利，星光不负赶路人！

---

### 如何配置与修改 AI 周报？

本系统会在**每周六晚 23:00** 自动运行，调用大语言模型（LLM）为您生成周报。要让它真正工作并使用您期望的模型，请按以下步骤操作：

#### 1. 在 GitHub 设置 Secrets (只需一次)

这是为了安全地存储您的 API 密钥。

1.  进入您的 GitHub 仓库主页，点击 **Settings** -> **Secrets and variables** -> **Actions**。
2.  点击 **New repository secret** 按钮。
3.  **Name**: `LLM_API_KEY`
4.  **Secret**: 粘贴您的 API 密钥。
5.  **(可选)** 如果您使用代理服务，请再添加一个 Secret：
    *   **Name**: `LLM_API_BASE_URL`
    *   **Secret**: 您的代理 URL。

*注：此设置与博客文章自动总结的爬虫共用，设置一次即可。*

#### 2. 修改 `weekly_report.yml` 工作流 (只需一次)

打开 `.github/workflows/weekly_report.yml` 文件，确保 `env` 部分正确引用了您设置的 Secrets。

```yaml
env:
  OPENAI_API_KEY: ${{ secrets.LLM_API_KEY }}
  OPENAI_API_BASE: ${{ secrets.LLM_API_BASE_URL }}
```

#### 3. 修改 `weekly_summary_generator.py` (核心)

这是激活真实调用和**更换模型**的地方。打开 `crawlers/weekly_summary_generator.py`，找到 `get_llm_summary` 函数。

1.  **激活真实调用**:
    *   找到 `response = client.chat.completions.create(...)` 和 `summary = response.choices[0].message.content` 这几行，**取消它们的注释**。
    *   找到 `summary = """..."""` 这个大的示例周报文本块，并将其**删除或用 `"""` 注释掉**。

2.  **如何替换模型**:
    *   在 `client.chat.completions.create(...)` 这一段中，找到 `model="..."` 这一行。
    *   将引号中的模型名称，替换为您想使用的任何模型。

    **修改前**:
    ```python
    # response = client.chat.completions.create(
    #     model="qwen2.5-max",
    #     ...
    # )
    ```

    **修改后 (例如换成 gpt-4o)**:
    ```python
    response = client.chat.completions.create(
        model="gpt-4o",
        ...
    )
    ```

完成以上步骤后，您的 AI 周报系统就会按时、使用您指定的模型，为您生成独一无二的分析报告。